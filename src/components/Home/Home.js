import React from 'react';
import { Link } from 'react-router-dom';
import Login from '../Login/Login';

const Home = () => {
    return (
        <div>
            <Login></Login>
        </div>
    );
};

export default Home;